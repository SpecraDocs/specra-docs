# Specra Docs - AI-Friendly Documentation

## Project Overview
specra-docs is the official documentation website for Specra. It serves as both comprehensive documentation for users and a reference implementation demonstrating Specra's capabilities.

## Key Information
- Project Name: specra-docs
- Type: Documentation Website + SaaS Platform / Next.js Application
- Purpose: Document Specra framework, demonstrate its features, and offer paid SaaS tiers
- URL: https://specra-docs.com (self-hosted with Caddy)
- Repository: https://github.com/dalmasonto/specra-docs
- Built With: Specra (dogfooding)

## Core Purpose
1. **Documentation**: Comprehensive guides, API references, and tutorials for Specra
2. **Reference Implementation**: Real-world example of Specra in production
3. **Showcase**: Demonstrates all Specra features and capabilities
4. **Onboarding**: Helps new users learn and adopt Specra

## Technology Stack
- Next.js 16.1.0 (App Router)
- React 19.2.3
- Specra 0.1.11 (the SDK this project documents)
- Tailwind CSS 4.1.9
- TypeScript 5
- Auth.js v5 (next-auth@beta) - Authentication (GitHub OAuth + credentials)
- Prisma v7 + PostgreSQL - Database ORM
- Stripe (Checkout + Webhooks + Customer Portal) - International payments (USD)
- M-Pesa Daraja API (STK Push) - Kenya payments (KES)
- bcryptjs - Password hashing
- Deployed self-hosted with Caddy reverse proxy

## Project Structure
```
specra-docs/
├── app/
│   ├── layout.tsx         # Root layout (uses specra)
│   ├── page.tsx           # Landing page
│   ├── globals.css        # Global styles
│   ├── not-found.tsx      # 404 page
│   ├── auth/              # Auth pages
│   │   ├── login/page.tsx
│   │   └── register/page.tsx
│   ├── pricing/page.tsx   # Public pricing page (4 tiers, USD/KES)
│   ├── dashboard/         # Authenticated dashboard
│   │   ├── layout.tsx     # Sidebar layout + auth guard
│   │   ├── page.tsx       # Overview
│   │   ├── billing/       # Subscription & payment history
│   │   └── settings/      # Account settings
│   ├── api/
│   │   ├── auth/[...nextauth]/route.ts  # Auth.js handler
│   │   ├── auth/register/route.ts       # User registration
│   │   ├── stripe/checkout/route.ts     # Stripe Checkout
│   │   ├── stripe/portal/route.ts       # Customer Portal
│   │   ├── webhooks/stripe/route.ts     # Stripe webhooks
│   │   └── mpesa/                       # M-Pesa endpoints
│   │       ├── stkpush/route.ts
│   │       ├── callback/route.ts
│   │       └── status/route.ts
│   └── docs/              # Documentation routes
│       └── [version]/[...slug]/page.tsx
├── lib/                   # Shared utilities
│   ├── db.ts              # Prisma client singleton
│   ├── auth-utils.ts      # Auth helpers
│   ├── stripe.ts          # Stripe client + plan pricing
│   └── mpesa.ts           # M-Pesa Daraja client
├── prisma/
│   ├── schema.prisma      # DB schema (User, Plan, Subscription, Payment...)
│   └── prisma.config.ts   # Prisma v7 datasource config
├── auth.ts                # Auth.js v5 configuration
├── middleware.ts           # Route protection
├── docs/                  # MDX documentation content
│   └── v1.0.0/
├── public/                # Static assets
├── scripts/               # Build and utility scripts
├── specra.config.json     # Specra configuration
├── next.config.mjs        # Next.js configuration
├── .env.local             # Environment variables (DB, Auth, Stripe, M-Pesa)
└── package.json
```

## Key Features
- Comprehensive Specra documentation
- Multi-version documentation support (v1.0.0)
- Tab groups for organized navigation (Guides, API Reference, Components)
- Dark mode support
- Responsive design
- Custom landing page
- Search functionality (MeiliSearch, currently disabled)
- Social links (GitHub, Twitter, Discord)
- Custom footer with links

### SaaS / Payment Features (added Feb 2026)
- 4 pricing tiers: Free, Starter ($19/mo), Pro ($49/mo), Enterprise ($149/mo)
- Dual payment providers: Stripe (international, USD) + M-Pesa (Kenya, KES)
- Annual billing discounts (save ~20%)
- Auth: GitHub OAuth + email/password via Auth.js v5
- User dashboard: plan overview, billing management, payment history
- Stripe Customer Portal: manage subscription, update payment method
- M-Pesa STK Push: phone-prompted payments for Kenyan users
- Webhook-driven subscription lifecycle (auto-activate, cancel, handle failures)
- Route protection middleware (dashboard requires authentication)

## Documentation Content Structure
```
docs/v1.0.0/
├── getting-started/      # Quick start guides
├── installation/         # Installation instructions
├── configuration/        # Configuration options
├── components/          # Component documentation
├── api/                 # API reference
├── guides/              # How-to guides
└── examples/           # Usage examples
```

## Configuration Highlights (specra.config.json)
- Site: Specra Docs at https://specra.vercel.app
- Theme: System-based (light/dark)
- Navigation: Sidebar, breadcrumbs, TOC enabled
- Tab Groups: 3 groups (Guides, API Reference, Components)
- Social: GitHub, Twitter, Discord links
- Search: MeiliSearch integration (disabled in current config)
- Footer: Custom links and branding

## Build Scripts
- `dev`: Development server (next dev)
- `build`: Production build with default config
- `build:export`: Static export build for GitHub Pages
- `start`: Production server
- `lint`: ESLint
- `generate:redirects`: Generate redirect rules
- `generate:static-redirects`: Generate static redirects for export
- `index:search`: Index content in MeiliSearch
- `test:search`: Test search functionality

## Deployment Options
1. **Self-hosted with Caddy (Production)**: `yarn build && yarn start` - Required for payment system
   - Caddy reverse proxies to localhost:3000
   - Supports API routes, Auth.js, webhooks
2. **GitHub Pages**: `npm run build:export` - Static site export (NO payment features)
3. **Vercel**: `npm run build` - Server-side rendering
4. **Netlify**: Compatible with standard build

## Static Export Configuration
For GitHub Pages deployment:
- Uses `NEXT_PUBLIC_BASE_PATH` environment variable
- Generates static HTML files
- Creates redirect files for routing
- Supports custom domain or subdirectory hosting

## Relationship to Other Projects
- **specra-sdk**: This project uses specra as a dependency to build itself
- **specra-cli**: Documented here as a tool users can use
- **Flow**: SDK provides functionality → Docs explain how to use it → CLI helps users start

**Circular Relationship**:
- Specra SDK enables documentation sites
- This site documents the SDK
- The site is built with the SDK it documents (dogfooding)

## Key Pages
1. **Landing Page** (app/page.tsx): Custom hero and features showcase
2. **Documentation** (docs/v1.0.0/*): All MDX content
3. **Pricing** (app/pricing/page.tsx): 4 tier cards, monthly/annual toggle, USD/KES
4. **Login** (app/auth/login/page.tsx): GitHub OAuth + email/password
5. **Register** (app/auth/register/page.tsx): Account creation
6. **Dashboard** (app/dashboard/page.tsx): Plan overview, status, payment method
7. **Billing** (app/dashboard/billing/page.tsx): Subscription details, payment history
8. **Settings** (app/dashboard/settings/page.tsx): Account settings
9. **404 Page** (app/not-found.tsx): Custom not found page

## Scripts Overview

### generate-redirects.mjs
Generates redirect rules for proper routing during build.

### generate-static-redirects.mjs
Creates static redirect files for exported sites (GitHub Pages).

### index-search.ts
Indexes MDX content into MeiliSearch for search functionality.

### test-search.ts
Tests search indexing and queries.

## Development Workflow
```bash
# Install dependencies
npm install

# Start development
npm run dev

# Open http://localhost:3000

# Build for production
npm run build

# Test production build
npm start
```

## Content Authoring
Documentation is written in MDX format:
```mdx
---
title: Page Title
description: Page description
tabGroup: guides
---

# Page Heading

Content with **Markdown** and <ReactComponents />
```

## Search Integration
MeiliSearch configuration (currently disabled):
- Host: http://localhost:7700
- API Key: aSampleMasterKey
- Index Name: docs

To enable:
1. Set `search.enabled: true` in specra.config.json
2. Run MeiliSearch instance
3. Run `npm run index:search` to index content
4. Search UI automatically appears

## Social Integration
- GitHub: https://github.com/dalmasonto/specra-docs
- Twitter: https://twitter.com/dalmasonto
- Discord: Community invite link
- Custom: Website link to craftfolio

## Analytics
Supports:
- Google Analytics (configurable)
- Plausible (configurable)

Currently empty in config, ready for integration.

## Customization
This site demonstrates how to:
- Customize landing page (app/page.tsx)
- Configure Specra (specra.config.json)
- Organize documentation (docs/ structure)
- Add tab groups for navigation
- Integrate social links
- Set up footer with custom links
- Support multiple deployment targets

## Use Cases
1. **Learning Specra**: Follow guides to understand features
2. **API Reference**: Look up configuration options and components
3. **Troubleshooting**: Find solutions to common problems
4. **Best Practices**: Learn recommended patterns
5. **Examples**: See working code snippets
6. **Reference Implementation**: Study how a real Specra site is built

## Payment System Architecture

### Database (Prisma v7 + PostgreSQL)
- Schema: prisma/schema.prisma (7 models, 5 enums)
- Config: prisma/prisma.config.ts (Prisma v7 requires this for datasource URL)
- Models: User, Account, Session, VerificationToken, Plan, Subscription, Payment
- Client singleton: lib/db.ts

### Authentication (Auth.js v5)
- Config: auth.ts (GitHub OAuth + credentials providers, JWT sessions, Prisma adapter)
- API: app/api/auth/[...nextauth]/route.ts
- Registration: app/api/auth/register/route.ts (bcrypt hashing)
- Middleware: middleware.ts (protects /dashboard/*, redirects auth pages if logged in)

### Stripe (International, USD)
- Client: lib/stripe.ts (API version 2026-01-28.clover)
- Checkout: app/api/stripe/checkout/route.ts
- Portal: app/api/stripe/portal/route.ts
- Webhooks: app/api/webhooks/stripe/route.ts
  - checkout.session.completed, customer.subscription.updated/deleted, invoice.payment_failed

### M-Pesa Daraja (Kenya, KES)
- Client: lib/mpesa.ts (OAuth, STK Push, status query, phone normalization)
- STK Push: app/api/mpesa/stkpush/route.ts
- Callback: app/api/mpesa/callback/route.ts
- Status: app/api/mpesa/status/route.ts

### Environment Variables (.env.local)
DATABASE_URL, AUTH_SECRET, AUTH_GITHUB_ID, AUTH_GITHUB_SECRET,
STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET,
MPESA_CONSUMER_KEY, MPESA_CONSUMER_SECRET, MPESA_SHORTCODE, MPESA_PASSKEY,
MPESA_CALLBACK_URL, MPESA_ENV, NEXT_PUBLIC_APP_URL

## Development Dependencies
- @tailwindcss/postcss: PostCSS plugin
- @tailwindcss/typography: Typography utilities
- prisma: Database ORM CLI (dev dependency)
- @types/bcryptjs: Type definitions
- tsx: TypeScript execution for scripts
- TypeScript types for Node, React

## Deployment Status
- Production: https://specra-docs.com
- Platform: Self-hosted server with Caddy reverse proxy
- Build mode: standalone (Next.js server at localhost:3000)
- Status: Live and active

## Why This Exists
1. **Self-Documentation**: Shows Specra users what they're learning
2. **Proof of Concept**: Demonstrates that Specra works in production
3. **Living Example**: Code can be inspected and learned from
4. **Quality Assurance**: Using Specra to document Specra catches bugs early

## Authors
dalmasonto, arthur-kamau

## Integration Points
- Uses specra SDK as primary dependency
- References create-specra CLI for user onboarding
- Deployed on Vercel for automatic CI/CD
- Open source for community contributions

## Key Insight
This project is both the product manual AND a working model built with the product itself - the ultimate form of dogfooding that validates Specra's capabilities.
