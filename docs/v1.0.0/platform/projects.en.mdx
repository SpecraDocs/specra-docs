---
title: Projects
description: Create and manage documentation projects with custom domains, GitHub integration, and built-in features
sidebar_position: 2
icon: folder
tab_group: platform
---

# Projects

A project is a single documentation site on the Specra Platform. Each project gets its own subdomain, deployment pipeline, analytics, and settings.

## Creating a Project

You can create a new project from the dashboard or using the CLI:

<Tabs defaultValue="Dashboard">
  <Tab label="Dashboard">
    1. Go to your [dashboard](https://specra-docs.com/dashboard)
    2. Click **New Project**
    3. Enter a project name — a subdomain is auto-generated from it
    4. Specra provisions a route and SSL certificate for your subdomain
    5. You're redirected to the project page, ready to deploy
  </Tab>
  <Tab label="CLI">
```bash
specra init my-docs
```
  This creates a new project and links your local directory to it.
  </Tab>
</Tabs>

<Callout type="info">
  When you create a project, Specra automatically provisions a Caddy route and SSL certificate for your subdomain. This takes a few seconds — you'll see a progress indicator while it completes.
</Callout>

## Subdomains and Custom Domains

Every project automatically gets a subdomain:

```
your-project.docs.specra-docs.com
```

The subdomain is provisioned with HTTPS during project creation, so it's ready to serve content as soon as you deploy.

### Custom Domains

You can connect your own domain to any project. Custom domains are available on all plans, including Free.

<Steps>
  <Step title="Add Your Domain">
    Go to **Project Settings > Domains** and enter your domain (e.g., `docs.yourcompany.com`).
  </Step>
  <Step title="Configure DNS">
    Add a CNAME record pointing to `cname.specra-docs.com` at your DNS provider.
  </Step>
  <Step title="Verify">
    Specra automatically verifies your DNS configuration and provisions an SSL certificate via Let's Encrypt.
  </Step>
</Steps>

<Callout type="info">
  SSL certificates are provisioned automatically and renewed before expiry. No manual configuration needed.
</Callout>

## Deploying Content

After creating a project, you need to deploy content before your site goes live. Until then, visitors to your subdomain will see a blank page.

<Tabs defaultValue="CLI">
  <Tab label="CLI">
```bash
cd my-docs
specra deploy
```
  The CLI builds your documentation locally and uploads it to Specra.
  </Tab>
  <Tab label="GitHub">
  Connect a GitHub repository to enable automatic deployments on every push. Available on **Pro** and **Enterprise** plans.

  1. Go to **Project Settings > GitHub**
  2. Authorize the Specra GitHub App
  3. Select your repository and branch
  4. Every push to the configured branch triggers a new deployment
  </Tab>
</Tabs>

<Callout type="tip">
  You can configure which branch triggers deployments. Most teams use `main` for production docs.
</Callout>

## Contact Forms

Projects on **Starter** plans and above can enable a built-in contact form on their documentation site. Form submissions are delivered to your configured email address and stored in the dashboard.

## Live Chat

**Pro** and **Enterprise** plans include an embeddable live chat widget. Visitors can ask questions directly from your docs, and your team responds from the Specra dashboard.

## Project Limits by Plan

| Feature | Free | Starter | Pro | Enterprise |
|---------|------|---------|-----|------------|
| Max projects | 1 | 4 | 20 | Unlimited |
| Custom domains | Yes | Yes | Yes | Yes |
| GitHub sync | - | - | Yes | Yes |
| Contact form | - | Yes | Yes | Yes |
| Live chat | - | - | Yes | Yes |
| Version history | 7 days | 30 days | Unlimited | Unlimited |
